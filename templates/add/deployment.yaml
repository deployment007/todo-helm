apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-add-task
spec:
  replicas: {{ .Values.addTask.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-add-task
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-add-task
    spec:
      containers:
      - name: {{ .Release.Name }}-add-task
        image: {{ .Values.common.acrURL }}/{{ .Values.addTask.image.name }}/{{ .Values.addTask.image.tag }}
        ports:
        - containerPort: {{ .Values.addTask.port }}
        env:
        - name: CONNECTION_STRING
          value: {{ .Values.common.connectionString }}