apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-delete-task
spec:
  ingressClassName: {{ .Values.common.ingressClassName }}
  rules:
  - host: {{ .Values.delete-task.ingress.host }}
    http:
        paths:
        - path: "/"
            pathType: Prefix
            backend:
            service:
                name: {{ .Release.Name }}-delete-task
                port:
                  number: {{ .Values.delete-task.servicePort }}