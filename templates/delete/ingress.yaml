apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ .Release.Name }}-delete-task
  annotations:
    kubernetes.io/ingress.class: {{ .Values.common.ingressClassName }}
spec:
  rules:
  - host: {{ .Values.deleteTask.ingress.host }}
    http:
        paths:
          - path: "/"
            pathType: Prefix
            backend:
                service:
                    name: {{ .Release.Name }}-delete-task
                    port:
                      number: {{ .Values.deleteTask.servicePort }}